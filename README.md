# Left-Rotation-ds The function calculates the new index for each element after d left rotations and places it in a new list. After processing all elements, it returns the rotated list as the result.
